<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eahm.exploringlibraries">
    <!--
        Uses:
        RetrofitClient.kt (1.3)
        RetrofitAdaptersRxJava.kt (8)
        RetrofitGson.kt (5)
        RetrofitRxJava.kt (4)
        RetrofitOAuth.kt (3)
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.RetrofitOAuth"/>
        <activity android:name=".ui.RetrofitRxJava" />
        <activity android:name=".ui.RetrofitGson" />
        <activity android:name=".ui.RetrofitAdaptersRxJava" />
        <activity android:name=".ui.RetrofitClient" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>